
<%= object.fields_for :sample_bottom_types do |builder| %>
  <%= builder.collection_select :bottom_type_id, BottomType.all, :id, :name, {:include_blank => ""} %>
  <%= builder.link_to_remove "Remove", {:tabindex => "-1"}  %>
<% end %>
