<%= form_for(@bottom_type) do |f| %>
  <% if @bottom_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bottom_type.errors.count, "error") %> prohibited this bottom_type from being saved:</h2>

      <ul>
      <% @bottom_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
